# Rename this file to .env and fill in your Messari API key
# The backend reads this via dotenv (loaded in app/app.js)

MESSARI_API_KEY=

# Optional: override the port (defaults to 3000)
# PORT=3000

